# server ??
server:
  port: 8079
  shutdown: graceful
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    session:
      timeout: 30m

# DB ??
spring:
  datasource:
    url: jdbc:sqlserver://172.20.22.223:1433;databaseName=ACE;encrypt=false
    username: ratel_backend
    password: mWNY$qSp7F
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  # Hibernate ??
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.SQLServerDialect
#        format_sql: true
#        show_sql: true
#        use_sql_comments: true
#        highlight_sql: true

logging:
  level:
    root: INFO
    org:
      springframework:
        web: DEBUG
        security: DEBUG
    com:
      example: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
